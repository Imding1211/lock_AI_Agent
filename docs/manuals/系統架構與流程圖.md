### 1. 系統架構與流程圖 (Mermaid)

```mermaid
graph TD
    %% 節點樣式定義
    classDef startEnd fill:#333,stroke:#fff,stroke-width:2px,color:#fff;
    classDef llmNode fill:#9370DB,stroke:#fff,stroke-width:2px,color:#fff;
    classDef decision fill:#ff9900,stroke:#333,stroke-width:2px;
    classDef dbNode fill:#4285f4,stroke:#fff,stroke-width:2px,color:#fff;
    classDef actionNode fill:#34a853,stroke:#fff,stroke-width:2px,color:#fff;
    classDef note fill:#f9f9f9,stroke:#666,stroke-dasharray: 5 5;

    %% 核心流程
    START((開始)):::startEnd --> pre_process["pre_process<br/>(載入 user profile, 注入摘要)"]:::actionNode
    pre_process --> manage_memory["manage_memory<br/>(語意摘要壓縮)"]:::llmNode
    manage_memory --> router["router<br/>(敏感詞攔截<br/>LLM 意圖分類<br/>直產轉接表單/拒絕語)"]:::llmNode

    %% 意圖分流（Send() fan-out，多意圖可平行派發）
    router -- "general_knowledge" --> PE["product_expert<br/>(產品規格專家)"]:::llmNode
    router -- "troubleshooting" --> TS["troubleshooter<br/>(故障排除專家)"]:::llmNode
    router -- "youtube_knowledge" --> YT["youtuber<br/>(YouTube教學影片專家)"]:::llmNode
    router -- "order_status" --> OC["order_clerk<br/>(訂單查詢專員)"]:::llmNode
    router -- "web_search" --> WR["web_researcher<br/>(網路搜尋助手)"]:::llmNode
    router -- "out_of_domain / transfer_human<br/>(Router 直接產生 answer)" --> merge_answers

    %% Agent 子圖（agent_llm ↔ tool_node 迴圈）
    PE --> PE_TOOL[("db_video<br/>pgvector")]:::dbNode
    TS --> TS_TOOL[("db_line_chat<br/>pgvector")]:::dbNode
    YT --> YT_TOOL[("db_youtube<br/>pgvector<br/>ui_type=VIDEO_CARD")]:::dbNode
    OC --> OC_TOOL[("db_order_api<br/>RESTful API")]:::dbNode
    WR --> WR_TOOL[("db_web_search<br/>DuckDuckGo")]:::dbNode

    PE_TOOL --> PE
    TS_TOOL --> TS
    YT_TOOL --> YT
    OC_TOOL --> OC
    WR_TOOL --> WR

    %% 匯流
    PE --> merge_answers["merge_answers<br/>(提取/合併回覆, 話題偵測)"]:::actionNode
    TS --> merge_answers
    YT --> merge_answers
    OC --> merge_answers
    WR --> merge_answers
    merge_answers --> update_profile["update_profile<br/>(LLM 萃取個資, 更新輪廓)"]:::llmNode
    update_profile --> post_process["post_process<br/>(Markdown 清洗, UI 工廠,<br/>FlexMessage 建構)"]:::actionNode
    post_process --> END((結束)):::startEnd

    %% 底層支援
    subgraph Support_Layer [工廠模式架構]
        direction LR
        f1[LLM Factory] -.-> manage_memory & router & PE & TS & YT & OC & WR & merge_answers & update_profile
        f2[Memory Factory<br/>PostgreSQL Checkpointer] -.-> START
        f3[Tool Factory<br/>+ UI_TYPE_MAP] -.-> PE_TOOL & TS_TOOL & YT_TOOL & OC_TOOL & WR_TOOL
        f4[Storage Factory<br/>Audit Log] -.-> START
        f5[Facts DB<br/>PostgreSQL user_facts<br/>SCD Type 2] -.-> pre_process & update_profile
        f6[UI Factory<br/>LINE FlexMessage] -.-> post_process
    end
    class Support_Layer note

```

---

### 2. 轉接真人策略

Agent 遵循「自主解決優先」原則，資訊不足時的處理順序：

1. 先提供能找到的部分資訊
2. 嘗試用不同關鍵字或角度重新檢索
3. 根據專業知識提供通用建議或常見做法
4. 建議使用者提供更具體的資訊
5. 只有當使用者明確表示以上方式都無法滿足需求時，才詢問是否需要轉接真人客服

`transfer_to_human` 工具僅在以下情況使用：
- 使用者明確堅持要求轉接真人客服
- 涉及安全風險（門鎖無法上鎖、疑似被破壞）

---

### 3. 專案目錄結構樹狀圖 (Directory Tree)

```text
lock_AI_Agent/
│
├── config.toml               # 系統核心設定檔 (驅動所有工廠、路由與 Agent 定義)
├── .env                      # 敏感環境變數 (API Keys 等)
├── main.py                   # CLI 測試入口
├── app.py                    # LINE Bot FastAPI webhook 入口（路由 + 生命週期）
│
├── core/                     # 核心設定解析
│   ├── __init__.py
│   ├── config.py             # TOML → Python 變數匯出
│   ├── constants.py          # 共用常數（PHONE_REGEX, ADDRESS_REGEX, SupportedUI 列舉，從 config 動態載入）
│   ├── line_bot.py           # LINE Messaging API 傳輸封裝（send_response 支援 Message 物件 / show_loading）
│   └── debounce.py           # 訊息防抖層與 LangGraph 流程控制（傳遞 response_ui）
│
├── graph/                    # LangGraph 對話流程
│   ├── __init__.py
│   ├── state.py              # GraphState TypedDict（messages, history, summary, ui_hints, response_ui...）
│   ├── nodes.py              # 節點邏輯（pre_process, manage_memory, router, merge_answers, update_profile, post_process + UI 工廠）
│   └── builder.py            # 組裝 StateGraph + Send() fan-out + 動態載入 Agent 子圖
│
├── agents/                   # 多 Agent 架構
│   ├── __init__.py           # build_agent_executor / build_all_agents（含 ui_hints metadata 攔截）
│   └── prompts/              # Agent 與 Router 的提示詞模板
│       ├── router.md
│       ├── product_expert.md
│       ├── troubleshooter.md
│       ├── youtuber.md
│       ├── order_clerk.md
│       ├── web_researcher.md
│       ├── merge_answers.md
│       ├── update_profile.md
│       ├── summarize_messages.md
│       └── transfer_human_form.md
│
├── tools/                    # 工具工廠（一檔案一工具）
│   ├── __init__.py           # REGISTRY + build_tools() + UI_TYPE_MAP
│   ├── base.py               # BaseTool ABC
│   ├── base_retriever.py     # BaseRetriever(BaseTool) 含 as_langchain_tool()
│   ├── chroma_store.py       # ChromaRetriever — Chroma 向量資料庫檢索
│   ├── pgvector_store.py     # PGVectorRetriever — pgvector 向量資料庫檢索（ui_type 時嵌入 metadata）
│   ├── api_store.py          # APIStoreRetriever — RESTful API 檢索
│   ├── web_search.py         # WebSearchRetriever — 網頁搜尋
│   ├── transfer_human.py     # TransferHumanTool — 轉接真人客服
│   └── line_ui_factory.py    # UI 工廠 — build_line_messages()（FlexMessage 影片卡片 / 輪播）
│
├── llms/                     # LLM 工廠
│   ├── __init__.py           # LLM_REGISTRY
│   ├── ollama_model.py
│   ├── gemini_model.py
│   └── vertexai_model.py
│
├── memory/                   # 對話記憶工廠
│   ├── __init__.py           # get_checkpointer()（MemorySaver / SQLite / Postgres）+ close_checkpointer()
│   ├── sqlite_saver.py       # AsyncSqliteSaver 封裝（SQLite 回退方案）
│   └── postgres_saver.py     # AsyncPostgresSaver 封裝（預設使用）
│
├── embeddings/               # Embedding 工廠
│   ├── __init__.py           # REGISTRY
│   ├── ollama_embed.py
│   └── vertexai_embed.py
│
├── profiles/                 # 使用者輪廓管理
│   └── manager.py            # ProfileManager（硬事實 → PostgreSQL user_facts SCD2 / 軟輪廓 → data/profiles/）
│
├── storage/                  # 審計日誌工廠
│   ├── __init__.py           # STORAGE_REGISTRY + get_storage() / close_storage()
│   ├── sqlite_impl.py        # SqliteAuditStorage（SQLite 回退方案）
│   └── postgres_impl.py      # PostgresAuditStorage（預設使用）
│
├── scripts/                  # 輔助腳本
│   ├── seed_db.py            # 向量資料庫初始化
│   ├── mock_api.py           # Mock 訂單 API
│   └── view_logs.py          # 審計日誌查看工具
│
├── data/                     # 動態資料（gitignore）
│   ├── db/                   # SQLite 回退用（預設使用 PostgreSQL，向量資料存於 pgvector）
│   └── profiles/             # 使用者輪廓
│
└── docs/                     # 開發文件
    ├── reports/              # 進度報告
    ├── manuals/              # 系統指南、開發手冊
    └── assets/               # 架構圖 (.mmd, .png)
```

---

### 4. 時序圖 — 單一意圖場景 (Sequence Diagram)

```mermaid
sequenceDiagram
    actor User as LINE 使用者
    participant App as app.py<br/>(防抖層)
    participant PP as pre_process
    participant MM as manage_memory
    participant RT as router
    participant Agent as Agent 子圖<br/>(e.g. product_expert)
    participant Tool as Tool<br/>(e.g. db_smartlock_manual)
    participant MA as merge_answers
    participant UP as update_profile
    participant Post as post_process
    participant LLM as LLM Engine
    participant Profile as User Profiles<br/>(./data/profiles/)
    participant PG as PostgreSQL<br/>(Checkpointer + Audit Log)

    User->>App: 傳送 LINE 訊息
    App->>App: 審計日誌：記錄 user_raw
    App->>App: 訊息緩衝 (buffer_wait)
    App->>App: 審計日誌：記錄 user (合併後)
    App->>PP: 合併後的 question

    PP->>Profile: 載入 user profile
    Profile-->>PP: 輪廓文字 (or empty)
    PP-->>MM: messages + user_profile + summary

    alt messages > 閾值 (6)
        MM->>LLM: 語意摘要壓縮 (summarize_messages.md)
        LLM-->>MM: 結構化摘要
        MM->>MM: RemoveMessage 刪除舊訊息，保留最近 2 對
    else messages <= 閾值
        MM->>MM: 跳過壓縮
    end
    MM-->>RT: state (含 summary)

    RT->>LLM: 意圖分類 (router.md prompt)
    LLM-->>RT: intent = "general_knowledge"
    RT->>RT: 狀態清洗 (只保留 summary + 當前問題)
    RT-->>Agent: Send(product_expert, clean_state)

    loop agent_llm ↔ tool_node
        Agent->>LLM: system prompt + messages
        LLM-->>Agent: tool_call (db_smartlock_manual)
        Agent->>Tool: 執行檢索
        Tool-->>Agent: 檢索結果
        Agent->>LLM: tool result
        LLM-->>Agent: 最終回覆 (文字)
    end

    Agent-->>MA: state (含 AI messages)
    MA->>MA: 提取 answer + 偵測 topic_resolved

    MA-->>UP: answer
    UP->>LLM: update_profile.md prompt
    LLM-->>UP: 更新後的輪廓
    UP->>Profile: 儲存輪廓

    UP-->>Post: state
    Post->>Post: Markdown 清洗
    Post->>Post: UI 工廠（ui_hints → FlexMessage / TextMessage）
    Post-->>App: answer + response_ui
    App->>App: 審計日誌：記錄 ai
    App->>User: Reply / Push API（支援 FlexMessage + TextMessage 混搭）
```

---

### 5. 時序圖 — 多意圖平行場景 (Sequence Diagram)

```mermaid
sequenceDiagram
    actor User as LINE 使用者
    participant App as app.py<br/>(防抖層)
    participant PP as pre_process
    participant MM as manage_memory
    participant RT as router
    participant PE as product_expert
    participant TS as troubleshooter
    participant Tool_PE as db_smartlock_manual
    participant Tool_TS as db_troubleshooting
    participant MA as merge_answers
    participant UP as update_profile
    participant Post as post_process
    participant LLM as LLM Engine
    participant Profile as User Profiles

    User->>App: "SL-3000 規格？指紋辨識不靈敏怎麼辦？"
    App->>PP: 合併後的 question

    PP->>Profile: 載入 user profile
    Profile-->>PP: 輪廓文字
    PP-->>MM: messages + user_profile

    MM->>MM: 檢查是否需要壓縮記憶
    MM-->>RT: state

    RT->>LLM: 意圖分類
    LLM-->>RT: ["general_knowledge", "troubleshooting"]
    RT->>RT: 狀態清洗 (只保留 summary + 當前問題)

    par Send() fan-out 平行派發
        RT->>PE: Send(product_expert, clean_state)
        RT->>TS: Send(troubleshooter, clean_state)
    end

    par Agent 平行執行
        PE->>LLM: product_expert prompt
        LLM-->>PE: tool_call
        PE->>Tool_PE: 檢索規格
        Tool_PE-->>PE: 結果
        PE->>LLM: tool result
        LLM-->>PE: 回覆 A (規格資訊)
    and
        TS->>LLM: troubleshooter prompt
        LLM-->>TS: tool_call
        TS->>Tool_TS: 檢索排除步驟
        Tool_TS-->>TS: 結果
        TS->>LLM: tool result
        LLM-->>TS: 回覆 B (排除步驟)
    end

    PE-->>MA: 回覆 A
    TS-->>MA: 回覆 B

    MA->>LLM: merge_answers.md prompt + 回覆 A + 回覆 B
    LLM-->>MA: 合併後的統一回覆

    MA-->>UP: 合併 answer
    UP->>LLM: update_profile.md prompt
    LLM-->>UP: 更新後的輪廓
    UP->>Profile: 儲存輪廓

    UP-->>Post: state
    Post->>Post: Markdown 清洗 + 記錄 chat_history
    Post-->>App: answer
    App->>User: Reply / Push API
```

---

### 6. 時序圖 — Router 直接處理場景（out_of_domain / transfer_human）

```mermaid
sequenceDiagram
    actor User as LINE 使用者
    participant App as app.py<br/>(防抖層)
    participant PP as pre_process
    participant MM as manage_memory
    participant RT as router
    participant TransferTool as TransferHumanTool
    participant MA as merge_answers
    participant UP as update_profile
    participant Post as post_process
    participant LLM as LLM Engine
    participant Profile as User Profiles

    User->>App: "我要轉接真人客服"
    App->>PP: question

    PP->>Profile: 載入 user profile
    Profile-->>PP: 輪廓 (含地址、電話等)
    PP-->>MM: messages + user_profile

    MM->>MM: 檢查是否需要壓縮記憶
    MM-->>RT: state

    RT->>LLM: 意圖分類
    LLM-->>RT: intent = "transfer_human"

    alt transfer_human
        RT->>TransferTool: generate_form(user_id, extra_text)
        TransferTool-->>RT: 格式化轉接表單 (含已知個資)
        RT->>RT: 設定 answer + topic_resolved + next_agents=[]
    else out_of_domain
        RT->>LLM: 禮貌拒絕 prompt
        LLM-->>RT: 拒絕回覆
        RT->>RT: 設定 answer + next_agents=[]
    end

    RT-->>MA: Send(merge_answers, state)
    MA->>MA: answer 已設定，直接使用

    MA-->>UP: answer
    UP->>LLM: update_profile.md prompt
    LLM-->>UP: 更新後的輪廓
    UP->>Profile: 儲存輪廓

    UP-->>Post: state
    Post->>Post: Markdown 清洗 + 記錄 chat_history
    Post-->>App: answer
    App->>User: 回覆訊息
```

---

### 7. 時序圖 — Agent 自主轉接場景 (Sequence Diagram)

```mermaid
sequenceDiagram
    actor User as LINE 使用者
    participant App as app.py<br/>(防抖層)
    participant PP as pre_process
    participant MM as manage_memory
    participant RT as router
    participant Agent as Agent 子圖<br/>(e.g. troubleshooter)
    participant Tool as transfer_to_human
    participant MA as merge_answers
    participant UP as update_profile
    participant Post as post_process
    participant LLM as LLM Engine
    participant Profile as User Profiles

    User->>App: "門鎖打不開，我要找真人客服！"
    App->>PP: question

    PP->>Profile: 載入 user profile
    Profile-->>PP: 輪廓 (含地址、電話等)
    PP-->>MM: messages + user_profile

    MM->>MM: 檢查是否需要壓縮記憶
    MM-->>RT: state

    RT->>LLM: 意圖分類
    LLM-->>RT: intent = "troubleshooting"
    RT->>RT: 狀態清洗
    RT-->>Agent: Send(troubleshooter, clean_state)

    Agent->>LLM: troubleshooter prompt + messages
    LLM-->>Agent: tool_call (transfer_to_human)
    Agent->>Tool: 執行轉接
    Tool-->>Agent: 格式化轉接文字 (含已知個資)
    Agent->>LLM: tool result
    LLM-->>Agent: 轉接回覆

    Agent-->>MA: state
    MA->>MA: 偵測 transfer_to_human → topic_resolved
    MA->>MA: 使用 tool 回傳的格式化文字作為 answer

    MA-->>UP: answer + topic_resolved
    UP->>LLM: update_profile.md prompt
    LLM-->>UP: 更新後的輪廓
    UP->>Profile: 儲存輪廓

    UP-->>Post: state
    Post->>Post: Markdown 清洗 + 記錄 chat_history + topic_resolved
    Post-->>App: answer
    App->>User: 轉接訊息 (含個資確認表單)
```
